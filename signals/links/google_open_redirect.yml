name: "Link: Google Open Redirect"
type: "query"
source: |
  any(body.links,
    (
        strings.icontains(.href_url.domain.root_domain, "google") and
        .href_url.path == "/url" and
        strings.istarts_with(.href_url.query_params, "q=")
    )
  )
