name: "Attachment: VBA macro calling executable"
type: "query"
source: |
  length(filter(attachments, (.file_extension in~ $file_extensions_macros or .file_extension in~ $file_extensions_common_archives)
      and any(file.explode(.),
          any(.scan.vba.hex, strings.ilike(., "*exe*")))
  ))