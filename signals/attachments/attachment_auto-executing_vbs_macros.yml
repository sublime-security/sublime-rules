name: "Attachment: Auto-executing vbs macros"
type: "query"
source: |
  length(filter(attachments, 
      .file_extension in~ $file_extensions_macros
      and any(file.oletools(.).macros.keywords, .type =~ "autoexec"))