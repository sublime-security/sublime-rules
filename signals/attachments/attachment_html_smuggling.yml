name: "Attachment: HTML smuggling"
type: "query"
source: |
  length(map(filter(attachments, .file_extension in~ ('html', 'htm') 
        and any(file.explode(.), 
            any(.scan.javascript.identifiers, . == "unescape"))), .file_name)))