name: "Attachment with links to free subdomain host"
type: "query"
source: |
  filter(
    map(attachments,
        map(beta.binexplode(.),
            map(filter(.scan.url.urls, .domain.root_domain in $free_subdomain_hosts), .url)
        )
    ),
    length(.) > 0
  )
severity: "medium"
