name: "Links in attachments"
type: "query"
source: |
  distinct(filter(
    map(attachments,
        map(beta.binexplode(.),
            map(.scan.url.urls, .url)
        )
    ),
    length(.) > 0
  ), .)
severity: "informational"
