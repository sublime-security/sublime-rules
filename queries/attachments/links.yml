name: "Attachment links"
type: "query"
source: |
  filter(
    map(attachments,
        map(beta.binexplode(.),
            map(.scan.url.urls, .url)
        )
    ),
    length(.) > 0
  )
severity: "informational"
