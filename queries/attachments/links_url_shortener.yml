name: "Links to URL shortener in attachments"
type: "query"
source: |
  distinct(filter(
    map(attachments,
        map(beta.binexplode(.),
            map(filter(.scan.url.urls, .domain.root_domain in $url_shorteners), .url)
        )
    ),
    length(.) > 0
  ), .)
severity: "medium"
