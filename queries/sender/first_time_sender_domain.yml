name: "First-time sender domain"
description: |
  Our organization has never received an email from the sender's domain.
type: "query"
source: |
  sender.email.domain.domain not in $sender_domains
severity: "low"
