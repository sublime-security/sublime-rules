name: "SPF: Temporary error"
type: "query"
source: |
  any(distinct(headers.hops, .received_spf.verdict is not null), strings.ilike(.received_spf.verdict, "temperror"))
severity: "medium"
tags:
  - "Sender authentication"
