name: "Archive containing executable file"
type: "query"
source: |
  map(attachments, 
      map(filter(file.explode(.), any(.flavors.yara, . == "mz_file")), strings.concat(..file_name, " contains ", .file_name,))
  )
severity: "medium"
