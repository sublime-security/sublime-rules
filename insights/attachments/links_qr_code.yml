name: "QR code embedded Links in attachments"
type: "query"
source: |
  map(filter(attachments, .file_type ...),
    map(filter(file.explode(.),
               .scan.qr.type == "url"
        ),
        .scan.qr.url.url
    )
  )

severity: "medium"
