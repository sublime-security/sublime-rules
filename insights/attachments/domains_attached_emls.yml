name: "Domains in Attached EMLs"
type: "query"
source: |
  filter(map(attachments,
             distinct(
                 map(file.parse_eml(.).body.links, .href_url.domain.domain), .)
         ),
         length(.) > 0
  )
severity: "informational"
