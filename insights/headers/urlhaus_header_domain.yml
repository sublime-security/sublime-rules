name: "URLhaus domain in headers"
type: "query"
source: |
  distinct(map(filter(headers.domain, 
               .root_domain in $abuse_ch_urlhaus_domains_trusted_reporters
               and .root_domain not in $alexa_1m), 
  .root_domain), .)
severity: "medium"
tags:
  - "Suspicious links"
