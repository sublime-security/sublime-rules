name: "Open redirect destinations"
type: "query"
source: |
  map(filter(body.links, any(.href_url.rewrite.encoders, strings.ends_with(., "_open_redirect"))), .href_url.url)
severity: "medium"
tags:
  - "Links"
