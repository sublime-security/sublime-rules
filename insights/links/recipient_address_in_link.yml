name: "Recipient email in link"
type: "query"
source: |
  distinct(map(filter(body.links, any(recipients.to, strings.icontains(..href_url.url, .email.email))), .href_url.url), .) and any(recipients.to, .email.domain.valid)
severity: "low"
tags:
  - "Suspicious links"
