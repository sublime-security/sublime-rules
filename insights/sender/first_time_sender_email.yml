name: "First-time sender email"
description: |
  Our organization has never received an email from the sender's email address.
type: "query"
source: |
  type.inbound
  and sender.email.email not in $sender_emails
severity: "low"
