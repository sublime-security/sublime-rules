name: "Rewrite and track links in malicious flagged messages"
description: "Rewrite and links in malicious flagged messages"
type: "triage_rule"
triage_flagged_messages: true
default_actions: ["track_link_clicks"]
source: |
  type.inbound
  and ml.attack_score().verdict == "malicious"
