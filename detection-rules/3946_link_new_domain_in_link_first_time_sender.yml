name: "PR# 3946 - New link domain (<=10d) from untrusted sender"
description: |
  Detects links in the body of an email where the linked domain is less than 10 days old from untrusted senders.
type: "rule"
severity: "medium"
source: |
  type.inbound
  and length(body.links) > 0
  and any(body.links, network.whois(.href_url.domain).days_old <= 10)
  and (
    (
      profile.by_sender().prevalence in ("new", "outlier")
      and not profile.by_sender().solicited
    )
    or profile.by_sender().any_messages_malicious_or_spam
  )
  // negate senders which have had previous messages marked as benign which pass auth
  and not (
    profile.by_sender().any_messages_benign
    and profile.by_sender().auth_failed == false
  )
tags:
  - "Attack surface reduction"
  - pr_author_zoomequipd
  - created_from_open_prs
  - rule_status_modified
attack_types:
  - "Credential Phishing"
  - "Malware/Ransomware"
detection_methods:
  - "Sender analysis"
  - "URL analysis"
  - "Whois"
id: "d22c735a-398a-5023-9f03-c87a6f3efb33"
references:
  - https://github.com/sublime-security/sublime-rules/pull/3946