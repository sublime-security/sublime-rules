name: "PR# 3941 - Service abuse: Google Forms with credential theft indicators"
description: "Detects messages containing credential theft language with a single link to a Google Forms document from inbound sources."
type: "rule"
severity: "medium"
source: |
  type.inbound
  and any(ml.nlu_classifier(body.current_thread.text).intents,
          .name == "cred_theft" and .confidence != "low"
  )
  and length(body.current_thread.links) == 1
  and body.current_thread.links[0].href_url.domain.domain == "docs.google.com"
  and strings.istarts_with(body.current_thread.links[0].href_url.path, '/form')
  // if there is a display url, it should account for less than 80% of the body.current_thread.text length
  and (
    length(body.current_thread.links[0].display_url.url) / (
      length(body.current_thread.text) * 1.0
    )
  ) < 0.8
  // negate replies
  and not (
    (subject.is_forward or subject.is_reply)
    and (length(headers.references) > 0 or headers.in_reply_to is not null)
  )
tags:
  - "Attack surface reduction"
  - pr_author_zoomequipd
  - created_from_open_prs
  - rule_status_added
attack_types:
  - "Credential Phishing"
tactics_and_techniques:
  - "Social engineering"
  - "Evasion"
detection_methods:
  - "Content analysis"
  - "Natural Language Understanding"
  - "URL analysis"
id: "9b03d33b-d008-5874-a340-0e49bd94ecb3"
references:
  - https://github.com/sublime-security/sublime-rules/pull/3941