name: "PR# 4953 - Credential theft: JavaScript date manipulation in HTML body"
description: "Detects inbound messages containing JavaScript that uses date manipulation functions (setDate/getDate) within script tags, combined with credential theft intent identified by NLU classification. This pattern is commonly used to evade detection by dynamically altering content or expiry logic while targeting user credentials."
type: "rule"
severity: "medium"
source: |
  type.inbound
  and regex.icontains(body.html.raw,
                      '<script[^>]*>[^<]*setDate\s*\(\s*[^<]*getDate\s*\(',
                      '<script[^>]*>[^<]*new\s+Date\s*\([^<]*(?:toLocaleDateString|toDateString|toISOString|toLocaleString)',
                      '<script[^>]*>[^<]*Date\.now\s*\('
  )
  and any(ml.nlu_classifier(body.current_thread.text).intents,
          .name == "cred_theft"
  )
attack_types:
  - "Credential Phishing"
tactics_and_techniques:
  - "Evasion"
  - "Scripting"
  - "Social engineering"
detection_methods:
  - "HTML analysis"
  - "Javascript analysis"
  - "Natural Language Understanding"
id: "03014341-edb7-5bae-b5bc-e7a62b0e28f8"
tags:
  - created_from_open_prs
  - rule_status_modified
  - pr_author_JFarina5
references:
  - https://github.com/sublime-security/sublime-rules/pull/4953