name: "Attachment: PDF with Microsoft OneNote form submission"
description: "Detects PDF attachments containing a single sharepoint URL referencing the senders personal OneNote used for form submision lure."
type: "rule"
severity: "medium"
source: |
  type.inbound
  and length(filter(attachments, .file_type == "pdf")) >= 1
  and any(attachments,
          .file_type == "pdf"
          and any(file.explode(.),
                  any(.scan.url.urls,
                      strings.icontains(.path,
                                        strings.concat("/:o:/p/",
                                                       sender.email.local_part
                                        )
                      )
                      or strings.icontains(.path, "/:o:/g/personal/")
                  )
          )
  )
attack_types:
  - "Credential Phishing"
tactics_and_techniques:
  - "PDF"
  - "Social engineering"
detection_methods:
  - "File analysis"
  - "Content analysis"
id: "95b987c0-112f-5371-bcf5-1aa8349f4387"
og_id: "0675bbc5-b322-5bd6-a4ea-61dc5346d603"
testing_pr: 3504
testing_sha: 0c192a48332f19f5b5c243090be764287ca60d34