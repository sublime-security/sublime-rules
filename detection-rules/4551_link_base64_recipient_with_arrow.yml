name: "PR# 4551 - Link: Base64 encoded recipient address in URL with arrow indicator"
description: "Detects messages containing links with arrow symbols in the display text where the recipient's email address is base64 encoded within the URL path."
type: "rule"
severity: "medium"
source: |
  type.inbound
  and any(body.current_thread.links,
          strings.ends_with(.display_text, '→')
          and any(strings.scan_base64(.href_url.path),
                  . == recipients.to[0].email.email
          )
  )

attack_types:
  - "Credential Phishing"
tactics_and_techniques:
  - "Evasion"
  - "Social engineering"
detection_methods:
  - "Content analysis"
  - "URL analysis"
id: "501812e7-008f-55d7-969f-c26d3bc0c498"
tags:
  - created_from_open_prs
  - rule_status_added
  - pr_author_D-Bolton
references:
  - https://github.com/sublime-security/sublime-rules/pull/4551