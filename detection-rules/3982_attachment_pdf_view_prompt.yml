name: "PR# 3982 - Attachment: Self-sender PDF with minimal content and view prompt"
description: "Detects messages where the sender and recipient are the same address with a PDF attachment containing only 'VIEW PDF' text and a standardized body message requesting to view the attachment."
type: "rule"
severity: "high"
source: |
  type.inbound
  // self sender
  and length(recipients.to) == 1
  and (
    sender.email.email == recipients.to[0].email.email
    or recipients.to[0].email.domain.valid == false
  )
  and strings.starts_with(body.current_thread.text, 'Please see attached')
  and any(filter(attachments, .file_type == 'pdf'),
          any(file.explode(.),
              .scan.strings.strings[0] == 'VIEW PDF'
              and length(.scan.strings.strings) == 1
          )
  )
attack_types:
  - "Credential Phishing"
  - "Malware/Ransomware"
tactics_and_techniques:
  - "PDF"
  - "Social engineering"
  - "Evasion"
detection_methods:
  - "Content analysis"
  - "File analysis"
  - "Sender analysis"
id: "79d8b848-0400-50ca-8e49-0e14379409a4"
tags:
  - created_from_open_prs
  - rule_status_modified
  - pr_author_mrobertsonris
references:
  - https://github.com/sublime-security/sublime-rules/pull/3982