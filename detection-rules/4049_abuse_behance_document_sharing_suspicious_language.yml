name: "PR# 4049 - Service abuse: Behance document sharing with suspicious language"
description: "Detects messages containing document sharing language with a single Behance gallery link, potentially indicating abuse of the legitimate Adobe Behance platform for malicious purposes."
type: "rule"
severity: "medium"
source: |
  type.inbound
  and strings.ilike(body.current_thread.text,
                    "*shared a",
                    "*document*",
                    "*access*",
                    "*proposal*",
                    "*specified link*"
  )
  and length(filter(body.current_thread.links,
                    .href_url.domain.root_domain == 'behance.net'
                    and strings.icontains(.href_url.path, '/gallery/')
                    and .display_url.domain.root_domain == 'behance.net'
                    and strings.icontains(.display_url.path, '/gallery/')
             )
  ) == 1

attack_types:
  - "Credential Phishing"
tactics_and_techniques:
  - "Free file host"
  - "Social engineering"
detection_methods:
  - "Content analysis"
  - "URL analysis"
id: "ee156c5b-4f81-51a2-afef-1266c7a27c1e"
tags:
  - created_from_open_prs
  - rule_status_added
  - pr_author_IndiaAce
references:
  - https://github.com/sublime-security/sublime-rules/pull/4049