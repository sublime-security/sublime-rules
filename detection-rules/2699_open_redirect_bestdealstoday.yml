name: "Open Redirect: bestdeals.today"
description: |
  Message contains use of the bestdeals.today open redirect. This has been exploited in the wild.
type: "rule"
severity: "medium"
source: |
  type.inbound
  and any(body.links,
      .href_url.domain.root_domain == "bestdeals.today"
      and strings.icontains(.href_url.path, '/redirect')
      and regex.icontains(.href_url.query_params, '(?:^|&)url=')
  )
  and sender.email.domain.root_domain != 'bestdeals.today'
  and headers.return_path.domain.root_domain != "bestdeals.today"

  // negate highly trusted sender domains unless they fail DMARC authentication
  and (
    (
      sender.email.domain.root_domain in $high_trust_sender_root_domains
      and not headers.auth_summary.dmarc.pass
    )
    or sender.email.domain.root_domain not in $high_trust_sender_root_domains
  )
attack_types:
  - "Credential Phishing"
  - "Malware/Ransomware"
tactics_and_techniques:
  - "Open redirect"
detection_methods:
  - "Sender analysis"
  - "URL analysis"
id: "cd7b19e6-cbbe-57b5-d206-f2438e064e16"
og_id: "666de100-4d0b-56ae-a6e8-b007ce31920f"
testing_pr: 2699
testing_sha: cbe0f095258fe3ce9d42be5ac9c91dfce9d2b631
