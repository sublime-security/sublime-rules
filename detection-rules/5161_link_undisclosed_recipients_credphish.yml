name: "PR# 5161 - Link: Credential phishing link with undisclosed recipients"
description: |
  This rule detects messages with "Undisclosed Recipients" that contain a link to a credential phishing page. 
type: "rule"
severity: "medium"
source: |
  type.inbound
  and (
    // No Recipients
    length(recipients.to) == 0
    or all(recipients.to, .display_name == "Undisclosed recipients")
  )
  and length(recipients.cc) == 0
  and length(recipients.bcc) == 0
  and (
    any(body.links,
        ml.link_analysis(.).credphish.disposition == "phishing"
        and ml.link_analysis(.).credphish.confidence in ("medium", "high")
    )
    or any(body.current_thread.links,
           regex.icount(.href_url.path, '\.[a-z]{2,}/') >= 2
           and not strings.icontains(.href_url.path, .href_url.domain.root_domain)
    )
  )
  // negate highly trusted sender domains unless they fail DMARC authentication
  and not (
    sender.email.domain.root_domain in $high_trust_sender_root_domains
    and coalesce(headers.auth_summary.dmarc.pass, false)
  )
  and not profile.by_sender().solicited
  and not profile.by_sender().any_messages_benign

attack_types:
  - "Credential Phishing"
tactics_and_techniques:
  - "Evasion"
detection_methods:
  - "Computer Vision"
  - "Header analysis"
  - "URL screenshot"
id: "28e1c2f1-e4ec-5fa7-9d07-fa38fc9a81cf"
tags:
  - created_from_open_prs
  - rule_status_modified
  - pr_author_JFarina5
references:
  - https://github.com/sublime-security/sublime-rules/pull/5161