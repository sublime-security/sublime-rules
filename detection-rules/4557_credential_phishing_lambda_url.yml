name: "PR# 4557 - Credential phishing: AWS Lambda URL with recipient targeting"
description: "Detects messages containing AWS Lambda URLs with the recipient's email address embedded in the fragment, indicating potential abuse of AWS Lambda services for targeted malicious activities."
type: "rule"
severity: "medium"
source: |
  type.inbound
  and any(recipients.to, sender.email.local_part == .email.domain.sld)
  and any(body.links,
          strings.icontains(.href_url.domain.domain, "lambda-url")
          and strings.icontains(.href_url.fragment, recipients.to[0].email.email)
  )
attack_types:
  - "Credential Phishing"
tactics_and_techniques:
  - "Free subdomain host"
  - "Social engineering"
detection_methods:
  - "URL analysis"
  - "Content analysis"
id: "4b5cc22c-b16c-54a1-ad13-0d0f5fa4fe83"
tags:
  - created_from_open_prs
  - rule_status_added
  - pr_author_markmsublime
references:
  - https://github.com/sublime-security/sublime-rules/pull/4557