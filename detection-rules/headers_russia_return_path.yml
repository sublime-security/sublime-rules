name: "Russia return-path TLD"
description: |
  The return-path header is a .ru TLD from an .
type: "rule"
severity: "low"
source: |
  type.inbound
  and headers.return_path.domain.tld == "ru"
  and sender.email.domain.root_domain != "corp.mail.ru"
  and sender.email.domain.root_domain != "calendar.yandex.ru"
  and (
    (
        and not profile.by_sender().solicited
    )
    or (
    )
  )

tags:
  - "Attack surface reduction"
attack_types:
  - "BEC/Fraud"
  - "Credential Phishing"
  - "Malware/Ransomware"
detection_methods:
  - "Header analysis"
id: "588b3954-c03a-57fb-b5a4-abf993a8c003"
