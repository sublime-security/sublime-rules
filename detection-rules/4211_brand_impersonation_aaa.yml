name: "PR# 4211 - Brand impersonation: Automobile assistance associations"
description: "Detects messages impersonating automobile associations (AAA, CAA, RAC, etc.) offering vehicle emergency kits or roadside assistance services from untrusted senders."
type: "rule"
severity: "high"
source: |
  type.inbound
  and (
    any(ml.nlu_classifier(body.current_thread.text).entities,
        .name == "org"
        and .text in~ (
          'AAA', // American Automobile Assoc.
          'RAC', // UK Royal Automobile Club
          'RAA', // Australia Royal Automotive Assoc.
          'CAA', // Canadian Automobile Assoc.
          'BCAA', // BC Automobile Assoc.
          'AMA', // Alberta Motor Assoc.
        )
    )
  )
  and regex.icontains(body.current_thread.text,
                      '(car|vehicle|motor|driver|emergency|road.?side|break.?down|assist|save|discount|complimentary|free).{0,10}kit'
  )
  // and the sender is not from high trust sender root domains
  and (
    (
      sender.email.domain.root_domain in $high_trust_sender_root_domains
      and not headers.auth_summary.dmarc.pass
    )
    or sender.email.domain.root_domain not in $high_trust_sender_root_domains
  )
  // negate newsletters and quarantine notifications
  and not (
    any(ml.nlu_classifier(body.current_thread.text).topics,
        .name in ("Newsletters and Digests") and .confidence != "low"
    )
    or strings.icontains(subject.subject, "quarantine")
  )


attack_types:
  - "Credential Phishing"
tactics_and_techniques:
  - "Impersonation: Brand"
  - "Social engineering"
detection_methods:
  - "Content analysis"
  - "Natural Language Understanding"
  - "Header analysis"
  - "Sender analysis"
id: "48e03c83-e8ad-5388-8e6f-d683ea4a8aa7"
tags:
  - created_from_open_prs
  - rule_status_added
  - pr_author_peterdj45
references:
  - https://github.com/sublime-security/sublime-rules/pull/4211