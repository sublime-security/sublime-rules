name: "PR# 4658 - HTML content with print styling and credential theft language"
description: "Detects messages containing specific HTML print styling directives combined with high or medium confidence credential theft language, often used to format malicious content for printing or display."
type: "rule"
severity: "high"
source: |
    type.inbound
    and strings.icontains(body.html.raw,
                          '<style>html {-webkit-print-color-adjust: exact} @media print {html, body {margin: 0; padding: 0; break-inside: avoid; page-break-inside: avoid}}'
    )
    and any(ml.nlu_classifier(body.current_thread.text).intents,
            .name == 'cred_theft' and .confidence != 'low'
    )

tags:
  - "Attack surface reduction"
  - pr_author_cybher0808
  - created_from_open_prs
  - rule_status_added
attack_types:
  - "Credential Phishing"
tactics_and_techniques:
  - "Evasion"
  - "HTML smuggling"
  - "Social engineering"
detection_methods:
  - "Content analysis"
  - "HTML analysis"
  - "Natural Language Understanding"
id: "0c84a722-f69a-53ed-ba96-44ca794adcd9"
references:
  - https://github.com/sublime-security/sublime-rules/pull/4658