name: "Sender in blocklisted senders list"
description: "Detects senders in the organizations blocklisted_senders list"
type: "rule"
severity: "low"
source: |
  type.inbound
  and sender.email.email in $blocklisted_senders
detection_methods:
  - "Sender analysis"
