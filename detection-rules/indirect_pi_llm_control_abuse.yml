name: "Indirect prompt injection: LLM control token abuse in message body"
description: "Detects messages containing fabricated delimiters, structured prompt boundaries, or LLM-specific control tokens (such as <|im_start|>, [INST], <<SYS>>) paired with instructions to send, forward, or exfiltrate data. These patterns are characteristic of indirect prompt injection attacks targeting AI mail assistants or LLM-integrated productivity tools, where adversarial instructions embedded in inbound messages attempt to hijack automated actions such as forwarding content to attacker-controlled destinations."
type: "rule"
severity: "high"
source: |
  type.inbound
  and (
    // fabricated delimiters
    regex.icontains(body.html.inner_text, '>>[}\]\)!]{2,}//')
    or regex.icontains(body.html.inner_text, '<<(?:end_context|user_query|end_user_query|start_prompt|end_prompt|end_program|end_processing|confirmation)>>')
  
    // chat-template or like turn-boundary tokens
    or regex.icontains(body.html.inner_text, '(?:<\|(?:im_start|im_end|system|user|assistant|end|endoftext)\|>|\[/?INST\]|<<SYS>>)[^<]{0,200}(?:send email|forward|exfiltrat|destination=|ignore (?:all|any|previous)|do not (?:mention|tell|reveal|disclose))')
    // llmail action-injection syntax: send email destination=...; body=...
    or regex.icontains(body.html.inner_text, 'send email destination=.{0,120}?body=')
  )
attack_types:
  - "BEC/Fraud"
tactics_and_techniques:
  - "Social engineering"
  - "Evasion"
detection_methods:
  - "Content analysis"
  - "HTML analysis"
