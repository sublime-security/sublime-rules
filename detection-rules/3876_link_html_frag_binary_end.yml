name: "PR# 3876 - Link: HTML file with suspicious binary fragment ending pattern"
description: "Detects links to HTML files containing fragments with a suspicious pattern of alphanumeric characters followed by a 5-digit binary sequence, commonly used in malicious URL structures."
type: "rule"
severity: "high"
source: |
  type.inbound
  and any(body.links,
          strings.ends_with(.href_url.path, '.html')
          and regex.contains(.href_url.fragment, '\-[a-z0-9]+[01]{5}$')
  )
attack_types:
  - "Credential Phishing"
tactics_and_techniques:
  - "Evasion"
detection_methods:
  - "URL analysis"
id: "cf13ca52-6969-5cf4-98da-658c349c37e2"
tags:
  - created_from_open_prs
  - rule_status_added
  - pr_author_zoomequipd
references:
  - https://github.com/sublime-security/sublime-rules/pull/3876