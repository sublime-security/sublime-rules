name: "PR# 4282 - Link: Landing page with search-ms protocol redirect"
description: "Detects messages containing URL shortener links that redirect to search-ms protocol queries, which can be used to execute local file searches on Windows systems."
type: "rule"
severity: "high"
source: |
  type.inbound
  and all(body.links, .href_url.domain.root_domain in $url_shorteners)
  and any(body.links,
          regex.icontains(ml.link_analysis(., mode="aggressive").final_dom.raw,
                          'search-ms:[^\;]*query=[^\;]+\.lnk'
          )
  )
attack_types:
  - "Malware/Ransomware"
tactics_and_techniques:
  - "Evasion"
  - "Scripting"
detection_methods:
  - "URL analysis"
  - "Threat intelligence"
id: "0ef8ca26-b348-5f0d-8d4a-4bb8aa70c309"
tags:
  - created_from_open_prs
  - rule_status_added
  - pr_author_peterdj45
references:
  - https://github.com/sublime-security/sublime-rules/pull/4282