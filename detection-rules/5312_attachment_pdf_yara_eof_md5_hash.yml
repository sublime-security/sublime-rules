name: "PR# 5312 - Attachment: PDF with EOF MD5 hash marker"
description: "Detects inbound messages containing a PDF attachment whose contents match a YARA signature identifying an MD5 hash appended near the end of file. This technique is commonly used to make each generated PDF unique, helping evade hash-based detection systems."
type: "rule"
severity: "high"
source: |
    type.inbound
    and any(filter(attachments, .file_type == "pdf"),
            any(file.explode(.),
                any(.scan.yara.matches, .name in ("pdf_eof_md5_hash"))
            )
    )
attack_types:
  - "Credential Phishing"
  - "Malware/Ransomware"
tactics_and_techniques:
  - "PDF"
  - "Evasion"
detection_methods:
  - "File analysis"
  - "YARA"
id: "2704b7f5-2855-5195-9cde-72d7c9096933"
tags:
  - created_from_open_prs
  - rule_status_added
  - pr_author_zoomequipd
references:
  - https://github.com/sublime-security/sublime-rules/pull/5312