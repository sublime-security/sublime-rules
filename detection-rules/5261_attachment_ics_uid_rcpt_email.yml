name: "Attachment: ICS calendar file with recipient address in UID field"
description: "Detects inbound messages containing ICS calendar attachments where the UID property matches the recipient's email address, indicating potential calendar-based social engineering."
type: "rule"
severity: "high"
source: |
  type.inbound
  and any(attachments,
          (
            .file_type == "ics"
            or (
              .file_extension == "ics"
              or .content_type in ("application/ics", "text/calendar")
            )
          )
          //
          // This rule makes use of a beta feature and is subject to change without notice
          // using the beta feature in custom rules is not suggested until it has been formally released
          //
          and any(beta.file.parse_ics(.).events,
                  .uid == recipients.to[0].email.email
          )
  )
  
attack_types:
  - "Credential Phishing"
tactics_and_techniques:
  - "Social engineering"
detection_methods:
  - "File analysis"
  - "Content analysis"
id: "74a54219-4cd0-5726-8993-375021a65fb8"
og_id: "507d814e-1db5-56ce-929f-13c4c78b345f"
testing_pr: 5261
testing_sha: d75563232246c4f79b466c528d32fcded21aa9e3