name: "Attachment: ICS file with excessive custom properties"
description: "ICS calendar attachment contains an unusually high number of custom X- properties, which may indicate attempts to hide malicious content or exploit calendar parsing vulnerabilities."
type: "rule"
severity: "medium"
source: |
  type.inbound
  and any(filter(attachments,
                 .file_type in~ ('ics')
                 or .content_type in ("text/calendar", "application/ics")
          ),
          any(file.explode(.),
              .depth == 0
              and length(filter(.scan.strings.strings,
                                strings.starts_with(., 'X-')
                         )
              ) > 35
          )
  )

attack_types:
  - "Malware/Ransomware"
tactics_and_techniques:
  - "Evasion"
detection_methods:
  - "File analysis"
  - "Content analysis"
id: "ce3dd576-73cf-5c23-acef-fd3caa89143b"
og_id: "3ac947bc-9db3-5505-9d7b-4cd22c9931fe"
testing_pr: 4206
testing_sha: a2150942a730d8f19de40323f9b758ed1d7afd23