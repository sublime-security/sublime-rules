name: "Impersonation: Organizational SLD found in Sender Local FTS"
type: "query"
description: |
  The Sender local contains an organizational SLD.
  And the sender's domain is not in org domains and first time sender.
source: |
  type.inbound
  and ( 
    any($org_slds, strings.contains(sender.email.local_part, .))
      and sender.email.domain.root_domain not in $org_domains
  )
  and (
    (
      sender.email.domain.root_domain in $free_email_providers
      and sender.email.email not in $sender_emails
    )
    or (
      sender.email.domain.root_domain not in $free_email_providers
      and sender.email.domain.domain not in $sender_domains
    )
  )
severity: "low"
tags:
  - "Organization impersonation"
  - "Suspicious sender"
