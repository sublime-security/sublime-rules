name: "Brand impersonation: Sompo Japan KII"
description: "Impersonation of Sompo Japan KII insurance company, typically used for credential theft or fraud."
type: "rule"
severity: "medium"
source: |
  type.inbound
  and (strings.ends_with(sender.email.email, "sompo-japan-kii.com")
  or 
    sender.email.email == "admin@sompo-japan-kii.com"
  )
  // negate highly trusted sender domains unless they fail DMARC authentication
  and (
    (
      sender.email.domain.root_domain in $high_trust_sender_root_domains
      and not headers.auth_summary.dmarc.pass
    )
    or sender.email.domain.root_domain not in $high_trust_sender_root_domains
  )

attack_types:
  - "BEC/Fraud"
tactics_and_techniques:
  - "Impersonation: Brand"
  - "Social engineering"
detection_methods:
  - "Header analysis"
  - "Sender analysis"
  - "Content analysis"
