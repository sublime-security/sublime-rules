name: "PR# 4138 - Attachment: SVG files with evasion elements"
description: "This rule identifies incoming SVG vector graphics files containing specific patterns: circle elements combined with either embedded images, QR codes, or filenames that match recipient information. Limited to three attachments. SVG circle elements have been used to obfuscate QR codes and bypass automated QR code scanning methods."
type: "rule"
severity: "high"
source: |
  type.inbound
  and length(attachments) < 4
  and any(attachments,
          (
            .file_extension == "svg"
            or .content_type in ("image/svg+xml")
            or .file_type == "svg"
          )
          and any(file.explode(.),
                  any(.scan.xml.tags, . == "circle")
                  and 1 of (
                    any(.scan.xml.tags, . == "image"),
                    .scan.qr.data is not null,
                    any(recipients.to,
                        strings.icontains(..file_name, .email.local_part)
                    ),
                    any(recipients.to,
                        strings.icontains(..file_name, .email.domain.sld)
                    )
                  )
          )
  )
tags:
  - "Attack surface reduction"
  - pr_author_MSAdministrator
  - created_from_open_prs
  - rule_status_modified
attack_types:
  - "Malware/Ransomware"
  - "Credential Phishing"
tactics_and_techniques:
  - "QR code"
  - "Image as content"
  - "Evasion"
detection_methods:
  - "File analysis"
  - "XML analysis"
  - "QR code analysis"
  - "Sender analysis"
id: "db046ed5-d853-5ef6-bad2-cf49ad999e98"
references:
  - https://github.com/sublime-security/sublime-rules/pull/4138