name: "TEMPLATE: Homoglyph / lookastrings.like sender domain"
description: |
  Sender's domain is a lookastrings.like of one of your domains.
type: "rule"
source: |
  type.inbound
  and (
      strings.ilevenshtein(sender.email.domain.domain, 'YOURDOMAIN1.COM') < 2
      or strings.ilevenshtein(sender.email.domain.domain, 'YOURDOMAIN2.COM') < 2
  )
  and sender.email.domain.domain not in $recipient_domains
tags:
  - "Suspicious sender"
  - "Lookastrings.like domain"
