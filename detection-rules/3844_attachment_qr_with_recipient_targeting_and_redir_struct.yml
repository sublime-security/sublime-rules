name: "PR# 3844 - Attachment: QR code with encoded recipient targeting and redirect indicators"
description: "Detects QR codes in attachments that contain the recipient's email address (either plaintext or base64 encoded) and redirect through suspicious URI structures commonly associated with Kratos/SneakyLog redirection services."
type: "rule"
severity: "high"
source: |
  type.inbound
  // QR code detection in attachments
  and any(attachments,
          // Office documents, PDFs, or image files
          (
            .file_type in $file_types_images
            or .file_extension in $file_extensions_macros
            or .file_type == "pdf"
          )
          and any(file.explode(.),
                  .scan.qr.type == "url"
                  // QR code URL contains recipient's email (targeting indicator)
                  and any(recipients.to,
                          .email.domain.valid
                          and (
                            // Plaintext email address in URL
                            strings.icontains(..scan.qr.url.url, .email.email)
                            // OR base64 encoded email address
                            or any(beta.scan_base64(..scan.qr.url.url,
                                                    format="url",
                                                    ignore_padding=true
                                   ),
                                   strings.icontains(., ..email.email)
                            )
                          )
                  )
                  // URI Struct for Common Kratos/SneakyLog Redir
                  and regex.contains(.scan.qr.url.url,
                                     '\.(?:c(?:ompany|you)|sbs)(?:[$#]|\?a=)'
                  )
          )
  )
attack_types:
  - "Credential Phishing"
tactics_and_techniques:
  - "QR code"
  - "Evasion"
  - "Image as content"
  - "Open redirect"
detection_methods:
  - "Archive analysis"
  - "File analysis"
  - "QR code analysis"
  - "URL analysis"
id: "bccd2aeb-851d-519a-b053-10d44251fe2b"
tags:
  - created_from_open_prs
  - rule_status_added
  - pr_author_hadojae
references:
  - https://github.com/sublime-security/sublime-rules/pull/3844