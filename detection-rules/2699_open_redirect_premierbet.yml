name: "Open Redirect: PremierBet"
description: |
  Message contains use of the PremierBet open redirect. This has been exploited in the wild.
type: "rule"
severity: "medium"
source: |
  type.inbound
  and any(body.links,
      .href_url.domain.root_domain == "premierbet.com"
      and strings.icontains(.href_url.query_params, 'returnUrl=')
  )
  and not sender.email.domain.root_domain == "premierbet.com"

  // negate highly trusted sender domains unless they fail DMARC authentication
  and (
    (
      sender.email.domain.root_domain in $high_trust_sender_root_domains
      and not headers.auth_summary.dmarc.pass
    )
    or sender.email.domain.root_domain not in $high_trust_sender_root_domains
  )
attack_types:
  - "Credential Phishing"
  - "Malware/Ransomware"
tactics_and_techniques:
  - "Open redirect"
detection_methods:
  - "Sender analysis"
  - "URL analysis"
id: "e998e6da-6ac7-5624-4b24-ef68b72912fe"
og_id: "0ad17224-969d-5404-9ff0-866241c93f4e"
testing_pr: 2699
testing_sha: cbe0f095258fe3ce9d42be5ac9c91dfce9d2b631
