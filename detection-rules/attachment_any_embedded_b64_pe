name: "Attachment: File with embedded base64-encoded executable"
description: |
  Attachment contains a base-64 encoded executable.
type: "rule"
severity: "high"
  type.inbound 
  and any(attachments,
    any(beta.binexplode(.), 
      any(.flavors.yara, . == 'base64_pe')) 
  )
  and sender.email.email not in $recipient_emails
tags:
  - "Suspicious attachment"
  - "HTML smuggling"
