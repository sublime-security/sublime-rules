name: "PR# 4306 - Link: Is.gd shortened URL with fragment matching subject"
description: "Detects messages containing is.gd shortened links where the URL fragment contains only digits that also appear in the email subject line, indicating potential targeted link tracking or social engineering tactics."
type: "rule"
severity: "medium"
source: |
  type.inbound
  and any(body.current_thread.links,
          .href_url.domain.root_domain == 'is.gd'
          and .href_url.fragment is not null
          and regex.match(.href_url.fragment, '^\d+$')
          and strings.contains(subject.subject, .href_url.fragment)
  )

attack_types:
  - "Credential Phishing"
  - "BEC/Fraud"
tactics_and_techniques:
  - "Evasion"
  - "Social engineering"
detection_methods:
  - "Content analysis"
  - "URL analysis"
  - "Header analysis"
id: "84f86500-3564-586d-85c6-de581004eddb"
tags:
  - created_from_open_prs
  - rule_status_added
  - pr_author_D-Bolton
references:
  - https://github.com/sublime-security/sublime-rules/pull/4306