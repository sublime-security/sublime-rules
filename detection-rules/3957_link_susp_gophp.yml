name: "PR# 3957 - Link: Suspicious go.php redirect with document lure"
description: "Detects links containing a PHP redirect endpoint with authentication parameters, commonly used in malicious redirects and unauthorized access attempts."
type: "rule"
severity: "medium"
source: |
  type.inbound
  and any(body.current_thread.links,
          strings.ends_with(.href_url.path, "go.php")
          and strings.starts_with(.href_url.query_params, "auth=")
          and strings.count(.href_url.path, "/") == 2
  )

attack_types:
  - "Credential Phishing"
tactics_and_techniques:
  - "Evasion"
  - "Social engineering"
detection_methods:
  - "Content analysis"
  - "URL analysis"
id: "8ced4f1d-e444-587f-a650-77f21bc9246e"
tags:
  - created_from_open_prs
  - rule_status_added
  - pr_author_keaton-sublime
references:
  - https://github.com/sublime-security/sublime-rules/pull/3957