name: "PR# 5151 - Attachment: ZIP filename mismatch"
description: "Detects inbound messages with ZIP attachments that exhibit a mismatch between the filename stored in the local file header and the filename in the central directory."
type: "rule"
severity: "low"
source: |
  type.inbound
  and any(filter(attachments, .file_type == "zip"),
          any(file.explode(.),
              any(.scan.yara.matches, .name == "zip_pklfh_cd_fname_mismatch")
          )
  )
tags:
  - "Attack surface reduction"
  - pr_author_keaton-sublime
  - created_from_open_prs
  - rule_status_added
attack_types:
  - "Malware/Ransomware"
tactics_and_techniques:
  - "Evasion"
detection_methods:
  - "Archive analysis"
  - "YARA"
  - "File analysis"
id: "6daa3e40-f936-56c8-acdb-07632111940f"
references:
  - https://github.com/sublime-security/sublime-rules/pull/5151