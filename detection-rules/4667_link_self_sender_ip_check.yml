name: "PR# 4667 - Link: Self-sender with IP geolocation check"
description: "Detects messages where the sender and recipient are the same address and the email contains a link that accesses ipinfo.io to gather IP geolocation information on the user."
type: "rule"
severity: "medium"
source: |
  type.inbound
  // self sender
  and (
    length(recipients.to) == 1
    and recipients.to[0].email.email == sender.email.email
  )
  and any(body.current_thread.links,
          .href_url.domain.root_domain != sender.email.domain.root_domain
          and any(ml.link_analysis(.).unique_urls_accessed,
                  .url == 'https://ipinfo.io/json'
          )
  )
attack_types:
  - "Credential Phishing"
tactics_and_techniques:
  - "Evasion"
  - "Social engineering"
detection_methods:
  - "Sender analysis"
  - "URL analysis"
id: "0a7aa42a-506a-5b19-ac07-45544c64d28b"
tags:
  - created_from_open_prs
  - rule_status_added
  - pr_author_D-Bolton
references:
  - https://github.com/sublime-security/sublime-rules/pull/4667