name: "Brand impersonation: Bank of America"
description: |
  Impersonation of Bank of America, usually for credential theft.
references:
  - "https://www.techrepublic.com/article/credential-phishing-attack-impersonates-bank-of-america/"
type: "rule"
severity: "high"
source: |
  type.inbound
  and (
    sender.display_name =~ 'bank of america'
    or strings.ilevenshtein(sender.display_name, 'bank of america') <= 2
    or strings.ilike(sender.display_name, "*bofa*")
    or strings.ilike(sender.email.domain.domain, '*bankofamerica*')
    or any(ml.nlu_classifier(body.current_thread.text).entities,
           .name == "sender"
           and strings.icontains(.text, 'bofa', 'bank of america')
    )
  )
  and sender.email.domain.root_domain not in~ (
    'bankofamerica.com',
    'transunion.com',
    'bofa.com',
    'bofamerchantservices.com'
  )
  and (
    sender.email.email not in $recipient_emails
    or (
      length(recipients.to) == 1
      and sender.email.email == recipients.to[0].email.email
    )
  )
  
  // negate highly trusted sender domains unless they fail DMARC authentication
  and not (
    sender.email.domain.root_domain in $high_trust_sender_root_domains
    and coalesce(headers.auth_summary.dmarc.pass, false)
  )
attack_types:
  - "Credential Phishing"
tactics_and_techniques:
  - "Impersonation: Brand"
  - "Lookalike domain"
  - "Social engineering"
detection_methods:
  - "Header analysis"
  - "Sender analysis"
id: "dba5085e-1cc2-511d-9584-f5b333b70f97"
og_id: "d2fc6ea1-0c8d-57e0-bcaa-732a2e6b76e4"
testing_pr: 5023
testing_sha: 7f55a9f8ef6396da2fd4f97a01d7752a2d063024