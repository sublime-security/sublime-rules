name: "beta.DLP: AWS Secret Key"
description: "Detects messages containing AWS secret keys."
type: "dlp"
severity: "high"
source: |
  type.outbound
  and (
    any(beta.ml_extract_sensitive_information(body.current_thread.text).elements,
        .type == "aws_secret_access_key" and .confidence in ("medium", "high")
    )
    or any(beta.ml_extract_sensitive_information(subject.subject).elements,
           .type == "aws_secret_access_key" and .confidence in ("medium", "high")
    )
    or any(attachments,
           any(file.explode(.),
               (
                 any(beta.ml_extract_sensitive_information(.scan.strings.raw).elements,
                     .type == "aws_secret_access_key"
                     and .confidence in ("medium", "high")
                 )
                 or any(beta.ml_extract_sensitive_information(.scan.ocr.raw).elements,
                        .type == "aws_secret_access_key"
                        and .confidence in ("medium", "high")
                 )
               )
           )
    )
  )
tags:
  - "Credentials"
attack_types:
  - "DLP: Credential Exposure"
detection_methods:
  - "Content analysis"
  - "Natural Language Understanding"
  - "File analysis"
  - "Optical Character Recognition"
id: "da36d3c8-a5b4-5ad4-b1f3-0ee97dc86782"
og_id: "a494b51c-4ddd-574b-99a3-5b45c6bf6076"
testing_pr: 0