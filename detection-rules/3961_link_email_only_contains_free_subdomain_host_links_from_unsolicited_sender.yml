name: "PR# 3961 - Link: Free subdomain host with unsolicited sender"
description: "Detects unsolicited messages containing links exclusively to free subdomain hosting platforms from senders not in high trust domains or those failing DMARC authentication."
type: "rule"
severity: "low"
source: |
  type.inbound
  // email has at least one link
  and 0 < length(body.links)
  // email is unsolicited
  and not profile.by_sender().solicited
  // email recipient is valid domain
  and recipients.to[0].email.domain.valid
  // all links in email are free subdomain hosts
  and all(body.links,
          .href_url.domain.root_domain in $free_subdomain_hosts
          // we dont want links to images or files
          and not regex.icontains(.href_url.path, '\.[a-z]{3,4}$')
          and not regex.icontains(.display_text, '\.[a-z]{3,4}$')
  )
  // negate highly trusted sender domains unless they fail DMARC authentication
  and (
    coalesce(sender.email.domain.root_domain in $high_trust_sender_root_domains
             and not headers.auth_summary.dmarc.pass,
             false
    )
    or sender.email.domain.root_domain not in $high_trust_sender_root_domains
  )
  // negate classifications we're not interested in
  and not (
    any(ml.nlu_classifier(body.current_thread.text).topics,
        .name in (
          "Newsletters and Digests",
          "Advertising and Promotions",
          "Educational and Research",
          "B2B Cold Outreach",
          "Health and Wellness",
          "Romance",
          "Sexually Explicit Messages",
          "Software and App Updates",
          "Acts of Violence"
        )
        and .confidence == "high"
    )
  )

attack_types:
  - "Credential Phishing"
tactics_and_techniques:
  - "Free subdomain host"
  - "Evasion"
detection_methods:
  - "Header analysis"
  - "Natural Language Understanding"
  - "Sender analysis"
  - "URL analysis"
id: "2e26e426-2864-5fbc-acf6-a24fb3477772"
tags:
  - created_from_open_prs
  - rule_status_added
  - pr_author_hadojae
references:
  - https://github.com/sublime-security/sublime-rules/pull/3961