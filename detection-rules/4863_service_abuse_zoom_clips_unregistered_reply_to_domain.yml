name: "PR# 4863 - Service abuse: Zoom Clips with unregistered reply-to domain"
description: "Detects messages sent through legitimate Zoom infrastructure sharing a clip, where the reply-to domain has no registered WHOIS record."
type: "rule"
severity: "low"
source: |
  type.inbound
  // Legitimate zoom sending infrastructure
  and sender.email.email == "no-reply@zoom.us"
  // sharing a clip
  and strings.starts_with(subject.base, 'Clips')
  // reply-to domain is not registered
  and (network.whois(headers.reply_to[0].email.domain).found == false)
tags:
  - "Attack surface reduction"
  - pr_author_D-Bolton
  - created_from_open_prs
  - rule_status_added
attack_types:
  - "Spam"
tactics_and_techniques:
  - "Social engineering"
  - "Evasion"
detection_methods:
  - "Sender analysis"
  - "Header analysis"
  - "Whois"
id: "56eb8be2-c645-5a1c-87b1-d2e53b33dfd0"
references:
  - https://github.com/sublime-security/sublime-rules/pull/4863