name: "PR# 3834 - Service abuse: Adobe Behance gallery with suspicious redirect"
description: "Detects messages containing a single Behance gallery link where the display URL shows Behance but the actual destination redirects elsewhere, indicating potential abuse of Adobe's Behance platform for malicious redirects."
type: "rule"
severity: "medium"
source: |
  type.inbound
  and length(body.links) > 0
  and any(body.links,
          .display_url.domain.root_domain == "behance.net"
          and strings.contains(.display_url.path, '/gallery')
  )
  and length(filter(body.links, .href_url.domain.root_domain == "behance.net")) == 1

attack_types:
  - "Credential Phishing"
  - "Malware/Ransomware"
tactics_and_techniques:
  - "Open redirect"
  - "Evasion"
detection_methods:
  - "URL analysis"
  - "Sender analysis"
id: "484b46a3-035f-5a75-af4b-4bcc97b3fbdc"
tags:
  - created_from_open_prs
  - rule_status_added
  - pr_author_IndiaAce
references:
  - https://github.com/sublime-security/sublime-rules/pull/3834