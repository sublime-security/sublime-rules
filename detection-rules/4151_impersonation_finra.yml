name: "PR# 4151 - Brand impersonation: FINRA"
description: |
  Impersonation of the Financial Industry Regulatory Authority (FINRA)
references:
  - "https://www.thinkadvisor.com/2020/05/04/finra-warns-of-email-phishing-scam/"
  - https://github.com/sublime-security/sublime-rules/pull/4151
type: "rule"
severity: "medium"
source: |
  type.inbound
  and (
    strings.ilevenshtein(sender.display_name, 'finra') <= 1
    or strings.ilevenshtein(sender.email.domain.sld, 'finra') <= 1
  )
  and any(ml.nlu_classifier(body.current_thread.text).entities,
          .name == "financial"
  )
  and length(ml.nlu_classifier(body.current_thread.text).intents) > 0
  and sender.email.domain.root_domain not in~ (
    'finra.org',
    'finrax.com',
    'finca.wine', // a wine company
    'finta.com' // unrelated domain caught by levenshtein 
  )

attack_types:
  - "Credential Phishing"
tactics_and_techniques:
  - "Impersonation: Brand"
  - "Lookalike domain"
  - "Social engineering"
detection_methods:
  - "Header analysis"
  - "Sender analysis"
id: "d92e7d95-839e-5c5c-a88f-1471069c323d"
tags:
  - created_from_open_prs
  - rule_status_modified
  - pr_author_MSAdministrator