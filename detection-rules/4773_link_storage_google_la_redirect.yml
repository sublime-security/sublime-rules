name: "PR# 4773 - Link: Google Cloud Storage redirect to external domain"
description: "Messages contain links hosted on storage.googleapis.com that, when followed, redirect to a destination outside of googleapis.com. This technique abuses Google Cloud Storage's trusted reputation to bypass link reputation checks, with the actual payload hosted on an unrelated external domain. Observed lures include parcel delivery notifications impersonating shipping carriers (UPS, GLS), gambling bonus offers, health product promotions, and storage quota warnings."
type: "rule"
severity: "medium"
source: |
  type.inbound
  and any(body.links,
          .href_url.domain.domain == "storage.googleapis.com"
          and (
            ml.link_analysis(., mode="aggressive").effective_url.domain.root_domain != "googleapis.com"
            and ml.link_analysis(., mode="aggressive").effective_url.domain.root_domain != ""
          )
  )
tags:
  - "Attack surface reduction"
  - pr_author_zoomequipd
  - created_from_open_prs
  - rule_status_added
attack_types:
  - "Credential Phishing"
  - "Spam"
  - "Malware/Ransomware"
tactics_and_techniques:
  - "Free file host"
  - "Open redirect"
  - "Impersonation: Brand"
  - "Social engineering"
  - "Evasion"
detection_methods:
  - "URL analysis"
  - "Threat intelligence"
id: "9d16b704-0ce7-5ce2-a5a8-02faed1b7d33"
references:
  - https://github.com/sublime-security/sublime-rules/pull/4773