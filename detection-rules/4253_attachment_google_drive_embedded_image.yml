name: "PR# 4253 - Attachment: Fake Google Drive embedded attachment"
description: "Detects messages with small image attachments that contain PDF-related text but are embedded images combined with attachment related language."
type: "rule"
severity: "medium"
source: |
  type.inbound
  // link as image filtered char count, image size & keywords
  and any(filter(attachments, .file_extension in $file_types_images),
          4 < length(beta.ocr(.).text) < 40
          and regex.icontains(beta.ocr(.).text, '\bpdf|pof\b')
          and beta.parse_exif(.).image_height < 130
          and beta.parse_exif(.).image_width < 190
          and strings.icontains(body.html.raw,
                                strings.concat("cid:", .content_id)
          )
  )
  and regex.icontains(body.current_thread.text, '(?:see|find) attached')
  and any(ml.nlu_classifier(body.current_thread.text).intents, .name != "benign")

attack_types:
  - "Credential Phishing"
  - "BEC/Fraud"
tactics_and_techniques:
  - "Evasion"
  - "Image as content"
  - "Social engineering"
detection_methods:
  - "Computer Vision"
  - "Content analysis"
  - "Exif analysis"
  - "File analysis"
  - "HTML analysis"
  - "Natural Language Understanding"
  - "Optical Character Recognition"
  - "URL analysis"
id: "1733ba58-f45a-5669-8915-484251b824e5"
tags:
  - created_from_open_prs
  - rule_status_added
  - pr_author_missingn0pe
references:
  - https://github.com/sublime-security/sublime-rules/pull/4253