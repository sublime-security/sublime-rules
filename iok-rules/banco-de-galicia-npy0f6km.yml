attack_types:
- Credential Phishing
- Brand Impersonation
description: 'Detects a different Banco de Galicia phishing kit deployed often on
  replit.com.


  Converted from IOK rule - original focuses on website analysis.'
detection_methods:
- URL analysis
- Sender analysis
- Content analysis
- Natural Language Understanding
- Header analysis
id: 522170f3-67d1-4d0b-8e9e-f48a69526466
name: 'IOK: Banco de Galicia Phishing Kit npy0f6km'
references:
- https://urlscan.io/result/ae6ab5a8-6965-4b44-996a-248636849607
severity: high
tactics_and_techniques:
- 'Impersonation: Brand'
tags:
- IOK_Converted
- kit
- target.banco_de_galicia
- target_country.argentina
type: triage
source: |
  type.inbound
    and any(triage.flagged_rules, any(.detection_methods, ..name == "URL analysis"))
    and any(body.links,
      strings.icontains(ml.link_analysis(.).final_dom.raw, "link rel=\"icon\" type=\"image/gif/png\" href=\"fis/titulo.png\"")
    )
    and any(body.links,
      strings.icontains(ml.link_analysis(.).final_dom.raw, "<title>Galicia</title>")
    )
    and any(body.links,
      strings.icontains(ml.link_analysis(.).final_dom.raw, "href=\"./fis/default.min.css\"")
      and strings.icontains(ml.link_analysis(.).final_dom.raw, "href=\"./fis/customcarousel.min.css\"")
    )
    and any(body.links,
      strings.icontains(ml.link_analysis(.).final_dom.raw, "form action=\"index2.php\" method=\"post\" id=\"form1\"")
    )
    and any(body.links,
      strings.icontains(ml.link_analysis(.).final_dom.raw, "fis/titl.png")
      and strings.icontains(ml.link_analysis(.).final_dom.raw, "fis/tecl.png")
      and strings.icontains(ml.link_analysis(.).final_dom.raw, "fis/logo.png")
      and strings.icontains(ml.link_analysis(.).final_dom.raw, "fis/burbu.png")
    )
    and (
      strings.icontains(subject.subject, "account")
      or strings.icontains(subject.subject, "verify")
      or strings.icontains(subject.subject, "suspended")
    )
