attack_types:
- Cryptocurrency Scam
description: 'Detects a malicious DApp that force redirects when it detects the

  use of a Chromium based browser. Pretends to be a liquidity mining

  platform while presenting fake audit reports.


  Converted from IOK rule - original focuses on website analysis.'
detection_methods:
- Content analysis
- Header analysis
- URL analysis
- Sender analysis
id: 9a692b1d-ef54-41bb-8a62-f3069a9b4f52
name: 'IOK: Fake crypto mining - noChromium'
references:
- https://urlscan.io/result/ca867e6b-1175-475a-8a0f-fdaf839f09cb/
- https://urlscan.io/result/581a66ed-dd74-45ff-81b0-ef3fd094bd8b/
- https://urlscan.io/result/3bf65041-f47e-4b4f-9b6b-997baa7ba1ce/
severity: high
tactics_and_techniques:
- Investment scam
tags:
- IOK_Converted
- cryptocurrency
type: triage
source: |
  type.inbound
    and any(triage.flagged_rules, any(.detection_methods, ..name == "URL analysis"))
    and any(body.links,
      strings.icontains(ml.link_analysis(.).final_dom.raw, "wusdt/css")
      and strings.icontains(ml.link_analysis(.).final_dom.raw, "wusdt/img")
      and strings.icontains(ml.link_analysis(.).final_dom.raw, "wusdt/ercjs")
      and strings.icontains(ml.link_analysis(.).final_dom.raw, "static/alert")
      and strings.icontains(ml.link_analysis(.).final_dom.raw, "wusdt/js")
    )
    and any(body.links,
      strings.icontains(ml.link_analysis(.).final_dom.raw, "window.chrome")
    )
    and (
      strings.icontains(subject.subject, "coinbase")
      or strings.icontains(subject.subject, "crypto")
    )
