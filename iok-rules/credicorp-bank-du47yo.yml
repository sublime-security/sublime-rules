attack_types:
- Credential Phishing
- Brand Impersonation
description: 'Detects a different phishing kit targeting Credicorp Bank.

  This was found as a result of this kit being deployed on Replit.


  Converted from IOK rule - original focuses on website analysis.'
detection_methods:
- Content analysis
- URL analysis
- Header analysis
- Natural Language Understanding
- Sender analysis
id: e04fa889-e27b-4e7c-a798-675cdf38e994
name: 'IOK: Credicorp Bank Phishing Kit Du47YO'
references:
- https://urlscan.io/result/b916a5de-739a-4937-ae66-b1769b95cbd9/
severity: high
tactics_and_techniques:
- 'Impersonation: Brand'
tags:
- IOK_Converted
- IOK_kit
- IOK_target.credicorp_bank
- IOK_target_country.panama
type: rule
source: |
  type.inbound
    and any(triage.flagged_rules, any(.detection_methods, ..name == "URL analysis"))
    and any(body.links,
      strings.icontains(ml.link_analysis(.).final_dom.raw, "<title>CREDICORP</title>")
    )
    and (
      strings.icontains(body.links, "fisaDesertAll.css")
      and strings.icontains(body.links, "fisaDesertLogin.css")
      and strings.icontains(body.links, "fisaDesertLoginCustom.css")
    )
    and any(body.links,
      strings.icontains(ml.link_analysis(.).final_dom.raw, "img src=\"./sonlosarchis/logo.png\"")
    )
    and (
      strings.icontains(subject.subject, "account")
      or strings.icontains(subject.subject, "verify")
      or strings.icontains(subject.subject, "suspended")
    )
    and (
      not profile.by_sender().solicited
      or (
        profile.by_sender().any_messages_malicious_or_spam
        and not profile.by_sender().any_false_positives
      )
    )
