name: "beta.DLP: Client Secret / API Key"
description: "Detects messages containing client secrets or API keys."
type: "dlp"
severity: "high"
attack_types:
  - "DLP: Credential Exposure"
tags:
  - "Exfiltration:T1048"
  - "CredentialAccess:T1552"
  - "CredentialAccess:T1528"
source: |
  type.outbound
  and (
    any(beta.ml_extract_sensitive_information(body.current_thread.text).elements,
      .type == "client_secret_api_key" and .confidence in ("medium", "high")
    )
    or any(beta.ml_extract_sensitive_information(subject.subject).elements,
      .type == "client_secret_api_key" and .confidence in ("medium", "high")
    )
    or any(attachments,
    any(file.explode(.),
      (
        any(beta.ml_extract_sensitive_information(.scan.strings.raw).elements,
          .type == "client_secret_api_key" and .confidence in ("medium", "high")
        )
        or any(beta.ml_extract_sensitive_information(.scan.ocr.raw).elements,
          .type == "client_secret_api_key" and .confidence in ("medium", "high")
        )
      )
    )
  )
  )
detection_methods:
  - "Named entity recognition"
  - "Attachment content analysis"
