name: "DLP: Canada Health Service Number"
description: "Detects messages containing Canadian provincial health service numbers."
type: "dlp"
severity: "high"
source: |
  type.outbound
  and any([body.current_thread.text, subject.subject],
          // Health card numbers vary by province, generally 10-12 digits
          regex.contains(., '\b\d{10,12}\b')
  )
  and any([body.current_thread.text, subject.subject],
          regex.icontains(., 'health\s+card|health\s+service|OHIP|RAMQ|MSP|AHCIP')
  )

detection_methods:
  - "Content analysis"
