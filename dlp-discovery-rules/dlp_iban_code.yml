name: "DLP: IBAN Code"
description: "Detects messages containing International Bank Account Numbers (IBAN)."
type: "dlp"
severity: "high"
source: |
  type.outbound
  and any(ml.extract_sensitive_information(),
    .type == "iban_code"
  )
detection_methods:
  - "Content analysis"
