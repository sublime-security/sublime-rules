name: "DLP: France Debit Card Number"
description: "Detects messages containing debit card numbers."
type: "dlp"
severity: "high"
source: |
  type.outbound
  and any(attachments,
          (
            .file_extension in~ $file_extensions_common_archives
            or .file_extension in~ ('.csv', '.txt', '.xlsx', '.xls')
          )
          and any(file.explode(.),
                  regex.contains(.scan.ocr.raw, '\b(?:\d{4}[\s\-]?){3}\d{1,7}\b')
          )
  )
  and (
    strings.icontains(sender.email.domain.tld, "fr")
    or strings.icontains(body.current_thread.text, "france")
  )

detection_methods:
  - "File analysis"
  - "Optical Character Recognition"
