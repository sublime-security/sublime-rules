name: "DLP: Ukraine Domestic Passport"
description: "Detects messages containing Ukrainian domestic passport numbers."
type: "dlp"
severity: "high"
source: |
  type.outbound
  and (
    any(beta.ml_extract_sensitive_information(body.current_thread.text),
          .type == "UA_DOMESTIC_PASSPORT"
          and .confidence in ("medium", "high")
    )
    or any(attachments,
      any(file.explode(.),
        any(.scan.strings.strings,
          any(beta.ml_extract_sensitive_information(.),
              .type == "UA_DOMESTIC_PASSPORT"
              and .confidence in ("medium", "high")
          )
        )
      )
    )
  )
data_categories:
  - "PII"
data_types:
  - "Passport"
regions:
  - "Ukraine"
compliance: []
detection_methods:
  - "Presidio"
  - "Content analysis"
id: "52202828-df8f-4d9a-b0da-257e47c9611d"
