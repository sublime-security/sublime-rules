name: "DLP: Luxembourg National ID (Non-Natural Persons)"
description: "Detects messages containing Luxembourg National Identification Numbers for non-natural persons."
type: "dlp"
severity: "medium"
source: |
  type.outbound
  and any([body.current_thread.text, subject.subject],
          // Luxembourg Non-Natural Person ID: 11 digits
          regex.contains(., '\b\d{11}\b')
  )
  and any([body.current_thread.text, subject.subject],
          regex.icontains(., 'matricule|company\s+ID|identification')
  )
  and (
    strings.icontains(sender.email.domain.tld, "lu")
    or strings.icontains(body.current_thread.text, "luxembourg")
  )

detection_methods:
  - "Content analysis"
