name: "beta.DLP: US ABA Routing Number"
description: "Detects messages containing US ABA routing numbers."
type: "dlp"
severity: "high"
source: |
  type.outbound
  and any(ml.extract_sensitive_information(),
    .type == "us_aba_routing_number"
  )
detection_methods:
  - "Content analysis"
