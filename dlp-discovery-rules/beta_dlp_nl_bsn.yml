name: "beta.DLP: Netherlands BSN / Tax ID"
description: "Detects messages containing Dutch Burger Service Numbers (BSN) or tax identification numbers."
type: "dlp"
severity: "high"
attack_types:
  - "DLP: Personal Data"
tags:
  - "Personal data"
  - "GDPR"
  - "Netherlands"
<<<<<<< HEAD
  - "EU"
=======
>>>>>>> 1d5cd445e (Add P2 DLP detection rules)
source: |
  //
  // This rule makes use of a beta feature and is subject to change without notice
  // using the beta feature in custom rules is not suggested until it has been formally released
  //
  type.outbound
  and (
    any(beta.ml_extract_sensitive_information(body.current_thread.text).elements,
<<<<<<< HEAD
        .type == "nl_bsn" and .confidence in ("medium", "high")
    )
    or any(beta.ml_extract_sensitive_information(subject.subject).elements,
           .type == "nl_bsn" and .confidence in ("medium", "high")
    )
    or any(attachments,
           any(file.explode(.),
               (
                 any(beta.ml_extract_sensitive_information(.scan.strings.raw).elements,
                     .type == "nl_bsn" and .confidence in ("medium", "high")
                 )
                 or any(beta.ml_extract_sensitive_information(.scan.ocr.raw).elements,
                        .type == "nl_bsn" and .confidence in ("medium", "high")
                 )
               )
           )
=======
      .type == "nl_bsn" and .confidence in ("medium", "high")
    )
    or any(beta.ml_extract_sensitive_information(subject.subject).elements,
      .type == "nl_bsn" and .confidence in ("medium", "high")
    )
    or any(attachments,
      any(file.explode(.),
        (
          any(beta.ml_extract_sensitive_information(.scan.strings.raw).elements,
            .type == "nl_bsn" and .confidence in ("medium", "high")
          )
          or any(beta.ml_extract_sensitive_information(.scan.ocr.raw).elements,
            .type == "nl_bsn" and .confidence in ("medium", "high")
          )
        )
      )
>>>>>>> 1d5cd445e (Add P2 DLP detection rules)
    )
  )
detection_methods:
  - "Content analysis"
  - "Natural Language Understanding"
  - "File analysis"
  - "Optical Character Recognition"
<<<<<<< HEAD
id: "04f12a5a-7a92-538c-b5ab-ebdd59662eea"
=======
>>>>>>> 1d5cd445e (Add P2 DLP detection rules)
